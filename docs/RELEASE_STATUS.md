# Release candidate status — 13 September 2026

Public static replay: https://commerce-support-agent.vercel.app

Seven genuine recorded OpenAI runs are available anonymously. Hosted Chromium, Firefox and WebKit checks passed with API requests blocked and only same-origin replay data fetched. Mobile sizing and keyboard navigation checks passed. Invite-only live execution is deployed on the new isolated Railway backend and is enabled. The OpenAI key is stored only in that backend's service variables, as explicitly approved.

Local verification: the final backend build's real-provider set passed 40/40 executions (estimated $0.059339 for that set), followed by a 40/40 exact-action audit of persisted amounts, addresses, decisions and simulated-only effects. Thirteen Python protocol/tool unit tests passed. Go domain/API/storage race tests passed using a dedicated PostgreSQL database. Real invited refund approval and refresh tests passed on all three browsers. Python dependency scans reported no known vulnerabilities; the Go scan became clean after upgrading affected pgx/x/text versions.

Private GitHub branch: `codex/initial-build`. Initial CI caught nondeterministic GraphQL schema field ordering; code generation now sorts the schema. Corrected Linux CI passed, including Docker builds and all offline checks ([verified application commit](https://github.com/sha-shank-03/commerce-support-agent/actions/runs/34739631047)). Complete CI logs were scanned without exposing credentials; no credential-pattern matches were found. No repository has been made public and no verified-release tag has been created.

Hosted verification: the actual refund flow passed in all three browsers, including refresh before approval. Carrier-browser and read-only MCP integrations ran inside the Linux backend. A pending approval survived a backend restart; wrong digests were denied, duplicate approval returned the same simulated receipt, the five-run allowance held, cross-reviewer reads were denied and revocation invalidated existing sessions. The live-disable switch was tested with an authenticated request and then live access restored. See `hosting-verification.json` for the check results.

The portfolio-only PostgreSQL service is private-network-only, with distinct app databases/roles and denied cross-database CONNECT. Both backend containers have verified serverless sleep settings, one vCPU/1 GB caps, `/ready` checks and three restart attempts. No plan upgrades or credit purchases occurred. The $10/month target is not a bill guarantee; see [hosting operations and measured startup usage](HOSTING.md).

Remaining gates: finish paired live evaluation/publication review, then merge/tag and change visibility. The artwork project's thirty real Claude evaluations and genuine proof recordings remain blocked by Anthropic API credit. Both repositories remain private and neither is represented as fully delivered. GitHub push-to-deploy access has not been configured; direct CLI deployments are working.
